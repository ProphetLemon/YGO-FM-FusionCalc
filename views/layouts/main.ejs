<!doctype html>
<html lang="<%= lang %>">
    <%- include('../partials/head', { pageTitle, pageStyles }) %>
    <body class="<%= bodyClass %> font-body flex flex-col min-h-screen">
        <%- include('../partials/navbar', { activeNav, lang, t }) %>
        <main class="flex-1">
            <%- include('../' + contentView, { lang, t, year }) %>
        </main>
        <%- include('../partials/footer', { t, year }) %>
        <%- include('../partials/i18n-bootstrap', { lang, catalog }) %>
        <%- include('../partials/scripts', { pageScripts }) %>
    </body>
</html>
